# Local environment / secrets — never commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store

# Runtime / logs
*.log
*.pid

# Local agent config (not part of the public artifact)
.claude/
# NOTE: CLAUDE.md / AGENTS.md ARE part of the public artifact — they are the
# AI-native launch guide. (Local gitnexus stubs were replaced; do not re-ignore.)
