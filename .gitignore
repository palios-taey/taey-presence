# Local environment / secrets — never commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store

# Runtime / logs
*.log
*.pid
serving/run/

# Local agent config (not part of the public artifact)
.claude/
# NOTE: CLAUDE.md / AGENTS.md ARE part of the public artifact — they are the
# AI-native launch guide. (Local gitnexus stubs were replaced; do not re-ignore.)

# Site-specific deployment config. The committed template is serving/fleet.env.example.
fleet.env
serving/fleet.env

# Operator-local known-credential deny-list for check_public_clean.sh (never commit real values)
serving/credential-denylist.txt

# Retired path components are history-only, not current public surface.
archive/
archived/
deprecated/
superseded/
obsolete/
legacy/
