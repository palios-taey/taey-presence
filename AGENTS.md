# Agents

The launch guide for this repo lives in [`CLAUDE.md`](CLAUDE.md) — it is written for a coding
agent bringing `taey-presence` up on a new machine (decision tree, verify-after-each-step, the
real failure modes, success criteria). Read it first, then act.

For architecture, the exact Redis-key contract, and honest scope (what's built vs. not), see
[`README.md`](README.md). For standing up a model endpoint on NVIDIA Spark/Thor hardware, see
[`serving/SERVING.md`](serving/SERVING.md).
