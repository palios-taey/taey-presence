# taey-presence runtime dependencies
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.26
redis>=5.0
python-dotenv>=1.0

# Public graph authority and Taey-seat transaction adapter, pinned to the
# independently accepted source merge.
dcm-mesh @ git+https://github.com/palios-taey/dcm.git@0ab99ce3ef79bccddc66d3e2b3a7c4380c130703

# Required by the soma_proxy `fetch_url` tool (serving/soma_proxy.py:1248) —
# trafilatura extracts article text from HTML, pypdf from PDFs. The imports are
# function-local and try/except-guarded, so without these the proxy still starts
# and every other tool works; `fetch_url` alone returns the string
# "fetch_url error: missing dependency (...)" instead of page text. That is the
# state production ran in until 2026-07-30 — the failure is a returned string,
# not a crash, so it never surfaced as an error.
trafilatura>=2.0
pypdf>=6.0
