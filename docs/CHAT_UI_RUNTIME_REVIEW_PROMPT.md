# Chat UI runtime review prompt

Status: exact public dispatch prompt for the active architecture review; not Taey operating authority.

Attach the destination-specific governance Bundle A first and a Bundle B containing the complete
[`CHAT_UI_RUNTIME_REVIEW_PACKET.md`](CHAT_UI_RUNTIME_REVIEW_PACKET.md) second. Then paste exactly:

> Read both attached files fully before answering. Audit Taey's current Chat UI control plane and decide the smallest
> reliable architecture that preserves per-platform YAML/driver/monitor isolation while producing a real production
> consultation promptly. Deliver every section required by the task packet, including the exact minimal Taey-facing
> instruction, action contract, production acceptance sequence, and explicit disagreements. Follow
> the governance, evidence, acceptance, and stop conditions in the attachments. If either attachment or either frozen
> public repository commit is unavailable or incomplete, state that and stop.

The packet builder records the two bundle hashes and this exact prompt in the local receipt. The Chat is not asked to
derive filesystem state, hashes, byte counts, UI state, or production measurements.
