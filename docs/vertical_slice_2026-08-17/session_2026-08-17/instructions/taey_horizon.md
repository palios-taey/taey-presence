# CARRY TO HORIZON (ChatGPT, :2) — review packet, attach 5 files

Same pattern you just executed successfully on :3. Attach, do not paste. The Ctrl+U dialog
route you found worked — use it again.

## Files, all in /home/mira/taey_runs/horizon_review/

  COVER.md                  ← read-first cover, explains what Horizon is being asked
  seat_ack_gate.diff        ← the implemented gate
  grok_declaration_spec.md  ← the spec it was built from
  return_contract_rule.md   ← the merged prompt rule
  manifest.json             ← taey-delegate collect over the other four

The manifest is already produced (collect exit 0, 4 artifacts). You do NOT need to run collect
again.

## Steps

1. observe :2 to see current state.
2. Attach all five files, one at a time, confirming the chip after each.
3. Type a short message: this is a review request from infra; Horizon was assigned to author
   the seat ACK gate order and the return-contract rule, infra authored both instead, and
   Horizon is being asked to review the substance and correct anything wrong. Point at COVER.md
   as the read-first file.
4. Send.
5. STOP. Do not wait for the reply. Report what you did.

## Report back

Which files got chips confirmed, that you sent, and anything that did not behave as expected.
Do not report a chip you did not observe. [Unknown] for anything you could not verify.
